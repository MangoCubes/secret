# Decides how data should be stored
# Options: MySQL, SQLite, NoSQL, JSON
storageType = JSON
# Disable access without HTTPS
enforceHTTPS = false
# Sets how long one can stay logged in without performing any actions
sessionTimeout = 600